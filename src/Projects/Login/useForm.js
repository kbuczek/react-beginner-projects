import { useStatem, useEffect } from "react";

const useForm = () => {
  const [values, setValues] = useState({
    login: "",
    password: "",
    radioOption: 1,
  });

  const [errors, setErrors] = useState({});
};
